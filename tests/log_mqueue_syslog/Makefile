include ../Makefile.tests_common

APPLICATION = syslog

CFLAGS += -DRCV_QUEUE_SIZE=8
CFLAGS += -DLSYSLOGD_FEATURE_INPUT_RIOT_IPC
CFLAGS += -DLSYSLOGD_FEATURE_OUTPUT_STDOUT
CFLAGS += -DLSYSLOGD_FEATURE_OUTPUT_REMOTE
CFLAGS += -DLSYSLOGD_RIOT_IPC_INPUT_STACKSIZE=4096
CFLAGS += -DTHREAD_STACKSIZE_MAIN=8096
USEMODULE += syslog
USEMODULE += shell
USEMODULE += shell_commands
USEMODULE += ps
USEMODULE += log_mqueue

BOARD_WHITELIST := native

TEST_ON_CI_WHITELIST += all
include $(RIOTBASE)/Makefile.include
