# This is an attempt to blacklist the boards that have MSP-430 core
# TODO: check this
# method `fflush()` is not defined for MSP-430 (#6445 will fix this)
CONFIG_CPU_CORE_MSP430=n

CONFIG_MOD_SHELL=y
CONFIG_MOD_SHELL_COMMANDS=y
