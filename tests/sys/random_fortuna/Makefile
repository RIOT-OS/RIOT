include ../Makefile.sys_common

USEMODULE += embunit
USEMODULE += random
USEMODULE += prng_fortuna

INCLUDES += -I$(RIOTBASE)/sys/random/fortuna

DISABLE_MODULE += auto_init_random

include $(RIOTBASE)/Makefile.include
