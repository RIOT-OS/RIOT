include ../Makefile.tests_common

BOARD := nucleo-f411re

USEMODULE += xtimer
USEMODULE += epd_bw_spi

CFLAGS += -DDISPLAY_X=200
CFLAGS += -DDISPLAY_Y=200
CFLAGS += -DEPD_BW_SPI_CONTROLLER=EPD_BW_SPI_CONTROLLER_IL3829
CFLAGS += -DEPD_BW_SPI_PARAM_CS=GPIO_PIN\(PORT_B,6\)
CFLAGS += -DEPD_BW_SPI_PARAM_DC=GPIO_PIN\(PORT_C,7\)
CFLAGS += -DEPD_BW_SPI_PARAM_RST=GPIO_PIN\(PORT_A,8\)
CFLAGS += -DEPD_BW_SPI_PARAM_BUSY=GPIO_PIN\(PORT_B,10\)

include $(RIOTBASE)/Makefile.include
