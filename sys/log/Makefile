DIRS += $(dir $(wildcard $(addsuffix /Makefile, $(USEMODULE))))

# make compile results end up in "log"
MODULE := log

#this uses the submodules mechanism
SUBMODULES = 1

include $(RIOTBASE)/Makefile.base
