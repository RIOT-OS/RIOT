CFLAGS += -DFIRMWARE_NUM_SLOTS=3
CFLAGS += -DFIRMWARE_SLOT0_SIZE=$(SLOT0_SIZE)
CFLAGS += -DFIRMWARE_SLOT1_SIZE=$(SLOT1_SIZE)
CFLAGS += -DFIRMWARE_SLOT2_SIZE=$(SLOT2_SIZE)

include $(RIOTBASE)/Makefile.base
