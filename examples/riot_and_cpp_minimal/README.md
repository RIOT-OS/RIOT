Using minimal C++ in a program with RIOT
======================================
This project demonstrates minimal C++ usage in an application with RIOT.

Makefile Options
----------------
* CXXEXFLAGS : user's extra flags used to build c++ files should be defined here (e.g -std=gnu++11).
