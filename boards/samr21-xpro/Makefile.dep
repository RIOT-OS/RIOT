ifneq (,$(filter gnrc_netdev_default netdev_default,$(USEMODULE)))
  USEMODULE += at86rf233
  CFLAGS += -DNETDEV_DEFAULT_AT86RF2XX
  CFLAGS += -DNETDEV_DEFAULT_PARAM_SET=0
endif

ifneq (,$(filter saul_default,$(USEMODULE)))
  USEMODULE += saul_gpio
endif
