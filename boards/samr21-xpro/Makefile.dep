ifneq (,$(filter gnrc_netif_default,$(USEMODULE)))
  USEMODULE += at86rf233
  USEMODULE += gnrc_nomac
endif

ifneq (,$(filter periph_hwrng,$(FEATURES_REQUIRED)))
  USEMODULE += at86rf233
  USEMODULE += gnrc
endif

ifneq (,$(filter saul_default,$(USEMODULE)))
  USEMODULE += saul_gpio
endif
