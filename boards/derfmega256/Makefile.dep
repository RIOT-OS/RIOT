USEMODULE += boards_common_atmega

ifneq (,$(filter eui_provider,$(USEMODULE)))
  FEATURES_REQUIRED += periph_eeprom
endif

# board only has a single network interface
DEFAULT_MODULE += netdev_single
