# SORT THIS ALPHABETICALLY BY BOARD NAME!
ifneq (,$(filter boards_common_adafruit-nrf52-bootloader,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/adafruit-nrf52-bootloader
endif
ifneq (,$(filter boards_common_arduino-due,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/arduino-due
endif
ifneq (,$(filter boards_common_arduino-mkr,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/arduino-mkr
endif
ifneq (,$(filter boards_common_arduino-zero,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/arduino-zero
endif
ifneq (,$(filter boards_common_atxmega,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/atxmega
endif
ifneq (,$(filter boards_common_e104-bt50xxa-tb,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/e104-bt50xxa-tb
endif
ifneq (,$(filter boards_common_esp32,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/esp32
endif
ifneq (,$(filter boards_common_esp32c3,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/esp32c3
endif
ifneq (,$(filter boards_common_esp32s2,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/esp32s2
endif
ifneq (,$(filter boards_common_esp32s3,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/esp32s3
endif
ifneq (,$(filter boards_common_esp32x,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/esp32x
endif
ifneq (,$(filter boards_common_microbit,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/microbit
endif
ifneq (,$(filter boards_common_msb-430,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/msb-430
endif
ifneq (,$(filter boards_common_nucleo,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/nucleo
endif
ifneq (,$(filter boards_common_nucleo64,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/nucleo64
endif
ifneq (,$(filter boards_common_particle-mesh,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/particle-mesh
endif
ifneq (,$(filter boards_common_qn908x,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/qn908x
endif
ifneq (,$(filter boards_common_remote,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/remote
endif
ifneq (,$(filter boards_common_samdx1-arduino-bootloader,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/samdx1-arduino-bootloader
endif
ifneq (,$(filter boards_common_silabs,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/silabs
endif
ifneq (,$(filter boards_common_slwstk6000b,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/slwstk6000b
endif
ifneq (,$(filter boards_common_sodaq,$(USEMODULE)))
  DIRS += $(RIOTBOARD)/common/sodaq
endif

include $(RIOTBASE)/Makefile.base
