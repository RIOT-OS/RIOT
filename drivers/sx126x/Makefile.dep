USEMODULE += iolist
USEMODULE += netdev_legacy_api
USEPKG += driver_sx126x
FEATURES_REQUIRED += periph_gpio_irq
USEMODULE += checksum

USEMODULE += ztimer_usec
USEMODULE += event_thread
USEMODULE += event_callback

CFLAGS += -DCONFIG_IEEE802154_DEFAULT_SUBGHZ_CHANNEL=1
CFLAGS += -DCONFIG_IEEE802154_DEFAULT_TXPOWER=22
CFLAGS += -DCONFIG_IEEE802154_DEFAULT_CHANNEL=1
CFLAGS += -DCONFIG_IEEE802154_DEFAULT_SUBGHZ_PAGE=3
CFLAGS += -DACK_TIMEOUT_SYMS=255
ifneq (,$(filter netdev,$(USEMODULE)))
USEMODULE += netdev_ieee802154_submac
USEMODULE += lora
  
endif

