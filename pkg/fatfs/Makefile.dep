ifneq (,$(filter fatfs_vfs,$(USEMODULE)))
  USEMODULE += fatfs
  USEMODULE += vfs
endif

ifneq (,$(filter fatfs,$(USEMODULE)))
  USEMODULE += fatfs_diskio_mtd
  USEMODULE += auto_init_storage
  USEMODULE += mtd

  # Use RTC for timestamps if available
  FEATURES_OPTIONAL += periph_rtc
endif
